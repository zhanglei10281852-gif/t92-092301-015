{
  "name": "elderly-meal-service-api",
  "version": "1.0.0",
  "private": true,
  "description": "社区老年人助餐服务管理平台 API（Express + SQLite）",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "init-db": "node src/init.js",
    "seed": "node src/seed.js",
    "build": "node --check src/server.js && node --check src/app.js && node --check src/db.js && node --check src/auth.js && node --check src/subsidy.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.10.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  }
}
